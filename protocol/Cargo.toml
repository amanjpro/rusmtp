[package]
name = "protocol"
version = "0.1.0-beta2"
authors = ["amanjpro <http://www.amanj.me>"]

[features]
vendored = ["native-tls/vendored"]

[dependencies]
log = "0.4"
native-tls = "0.2"
base64 = "0.10"
common = { path = "../common" }
